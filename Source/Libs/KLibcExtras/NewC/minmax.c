/*
** select minimum of two variables
*/

min(v1, v2)
int   v1, v2;
   {
   return ((v1 > v2) ? v2 : v1);
   }


/*
** select maximum of two variables
*/

max(v1, v2)
int   v1, v2;
   {
   return ((v1 < v2) ? v2 : v1);
   }


